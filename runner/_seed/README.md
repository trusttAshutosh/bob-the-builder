# Seed templates for BOB_HOME

Copied into `assets/` on first `bob install` (empty catalogs).

| Seed | Live path |
|------|-----------|
| `api-catalog/` | `assets/api-catalog/` — fill with `bob discover-apis` |
| `stub-registry/` | `assets/stub-registry/` |
| `platform-graph/` | `assets/platform-graph/` — fill with `bob sync-graph` |
| `assertion-catalog/` | `assets/assertion-catalog/` |

Novopay CC reference snapshot (not seeded): `assets/examples/novopay-cc/`.
