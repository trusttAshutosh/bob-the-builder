# Assertion catalog (local Bob home)

Create `assertion-catalog/<feature>.yaml` under `{BOB_HOME}` when a ticket needs shared DB presets.

Tickets reference presets via `impacted.feature` and `db.assertion_preset` in `ticket-spec.yaml`.
