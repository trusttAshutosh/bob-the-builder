# Optional snippet: CC reads message.broker.bootstrap.servers from application.properties.
# Bob validate-ticket with run.kafka.enabled overrides this via bootRun args instead.
message.broker.bootstrap.servers=localhost:9092
