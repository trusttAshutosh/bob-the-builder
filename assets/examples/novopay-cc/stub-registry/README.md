# CC stub registry (reference snapshot)

**Not the live `BOB_HOME` registry.** Active stubs live in `../../stub-registry/`.

Copy bank-operation fixtures from here when needed — see [`../README.md`](../README.md).
