# CC API catalog (reference snapshot)

**Not the live `BOB_HOME` catalog.** Active APIs live in `../../api-catalog/` after `bob discover-apis`.

This tree is a Novopay credit-card orchestration snapshot for copy/compare. See [`../README.md`](../README.md).
